export function AppFooter() {
  return (
    <footer className="mt-16 pt-6 pb-4 border-t border-border text-center">
      <p className="text-xs text-muted-foreground">
        © {new Date().getFullYear()} Constantin Patrick-Alexandru FSEGA IE - Toate drepturile rezervate.
      </p>
    </footer>
  );
}
